from setuptools import setup, find_packages

setup(
    name='FreeFang',
    version='0.1.0',
    author='FreeFang Development Team',
    description='',
    packages=find_packages(),
    install_requires=[
    ],
)

