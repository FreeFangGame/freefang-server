from setuptools import setup, find_packages

setup(
    name='FreeFang',
    version='0.1.0',
    author='Solirs',
    description='',
    packages=find_packages(),
    install_requires=[
        'Flask',
    ],
)

